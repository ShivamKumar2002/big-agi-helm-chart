apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "big-agi.fullname" . }}-test-connection"
  labels:
    {{- include "big-agi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "big-agi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
